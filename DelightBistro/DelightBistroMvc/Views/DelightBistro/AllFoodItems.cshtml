@using DelightBistroMvc.Models.DelightBistro;
@model AllFoodItemWithPermissionViewModel

@{
    Layout = "_LayoutDelightBistro";
}

@section Scripts {
    <script src="/js/delight-bistro/all-foods.js"></script>
    @* <script src="/js/delight-bistro/buy-button.js"></script> *@

    <link rel="stylesheet" href="/css/delight-bistro/style.css" />
}
 
<header class="head-menu">
    <div class="logo-container">
        <div class="logo">
            <img src="/images/delight-bistro/logo.png" alt="logo" />
        </div>
        <div class="logo-name">Logo name</div>
    </div>
    <div class="main-info center">
        <div class="head-menu-item">Info</div>
        <div class="head-menu-item">Head menu</div>
    </div>
</header>

<h1 class="center">Кухня</h1>
<div>
    <a href="/DelightBistro/GenerateTable">Download table</a>
</div>


<div class="layout">
    <div class="plate">
                
        <div class="type-of-food">
            <p class="button">
                <button class="remove-food-item" disabled title="Удалить">Delete dishes</button>
            </p>
            <div class="type-name center">Все блюда</div>
            <div class="menu">
              
                @foreach (var foodItem in Model.FoodItems)
                {
                    <div class="food-item" item-id="@foodItem.Id">
                        <div class="image-container">
                            <a href="\DelightBistro\FoodBuilderData?id=@foodItem.Id"><img src=@foodItem.ImgURL alt=@foodItem.Name /></a>
                        </div>
                        <div class="name-price-btn">
                            <div class="name-price">
                                <div class="food-name">@foodItem.Name</div>
                                <div class="price">@foodItem.Price р</div>
                                @if (foodItem.CanDelete)
                                {
                                    <form action="/DelightBistro/DeleteFoodItem" method="post">
                                        <input type="hidden" name="id" value="@foodItem.Id" />
                                        <button type="submit" title="Удалить" class="delete-food-item">Delete</button>
                                    </form>

                                }
                            </div>
                            @* <div class="btnright">
                                <button data-food-item-id="@food.Id" class="buy-button colorbtn">+</button>
                            </div> *@
                        </div>
                        <div class="recept">
                            <ul>
                                @if (foodItem.IngredientsList is not null)
                                {
                                    @foreach (var ingredient in foodItem.IngredientsList)
                                    {
                                        <li>@ingredient.Name @ingredient.Quantity gr.</li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
        
    <aside class="right-bar">
        <div class="form-field">
            <form method="post">
                <div class="userName">Username</div>
                <input type="text"
                       name="username"
                       placeholder="Введите логин"
                       required />

                <div>Password</div>
                <input type="password"
                       name="userPassword"
                       placeholder="Введите пароль"
                       required />
                <button type="submit">Войти</button>
            </form>
        </div>

        <ul class="navigation">
            <li><a href="\DelightBistro\Index">Все меню</a> </li>
        </ul>
    </aside>
</div>

